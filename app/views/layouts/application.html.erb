<!DOCTYPE html>
<html>
  <head>
    <title>Wibio</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <%= render 'application/favicon' %>
  </head>

  <body>
    <main class="relative overflow-hidden max-w-7xl mx-auto">
      <!-- alerts and notices -->
      <% flash.each do |type, message| %>
        <% if type == 'alert' %>
          <div class="absolute top-0 inset-x-0 mx-auto p-2 max-w-md">
            <div class="flex items-center border border-gray-200 bg-white shadow-md px-1.5 text-sm rounded-md mb-3" role="alert" aria-live="assertive" aria-atomic="true">
              <div class="inline-flex items-center justify-center flex-shrink-0 w-8 h-8 text-tertiary-dark bg-tertiary-light rounded-md">
                <svg class="w-5 h-5" fill="currentColor" viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path fill-rule="evenodd" d="M8.257 3.099c.765-1.36 2.722-1.36 3.486 0l5.58 9.92c.75 1.334-.213 2.98-1.742 2.98H4.42c-1.53 0-2.493-1.646-1.743-2.98l5.58-9.92zM11 13a1 1 0 11-2 0 1 1 0 012 0zm-1-8a1 1 0 00-1 1v3a1 1 0 002 0V6a1 1 0 00-1-1z" clip-rule="evenodd"></path></svg>
              </div>
              <div class="p-3 rounded-b-lg break-words text-gray-700">
                <%= message %>
              </div>
            </div>
          </div>
        <% else %>
        <% end %>
      <% end %>

      <%= yield %>
    </main>
  </body>
</html>
