<div class="min-h-screen flex flex-col justify-center">
  <div class="grid grid-cols-1 md:grid-cols-2 gap-4">

    <div class="py-8 p-4 sm:px-6 md:px-8 lg:px-12 space-y-8 flex flex-col justify-center items-center md:items-baseline">
        <%= image_tag "logos/wibio.svg", class: "h-7", alt: "Wibio logo"  %>

        <div class="text-center sm:text-left">
          <h1 class="mt-8">
            <span class="block xl:inline">Welcome!</span>
            <span class="bg-gradient-to-r from-tertiary via-tertiary to-tertiary-light rounded-lg px-1 box-decoration-clone">We are glad to have you on board.</span>
          </h1>
          <p class="mt-3 text-base text-gray-800">
            Finally, you don't need to remember the names of all your social media accounts.
            Now you have only one single link that you can use and share anywhere.
            <strong>But first, let’s finish your profile!</strong>
          </p>
        </div>

        <div class="self-stretch">
          <%= form_with model: @user, url: finish_welcome_path, method: :post do |f| %>

              <div class="flex flex-col gap-6">

                <div class="w-full flex flex-col sm:flex-row items-center gap-4">
                  <%= render Forms::FileAvatarComponent.new(user: @user, form: f) %>
                  <div class="w-full">
                    <%= f.label :about, class: "label" %>
                    <%= f.text_area :about, placeholder: "It’s all about you. Be bold in 140 characters.", class: "input shadow-sm" %>
                  </div>
                </div>

                <div class="mt-5 sm:mt-8 flex flex-col sm:flex-row lg:justify-start gap-2">
                  <%= f.submit "Finish Profile", class:"w-full md:w-auto btn btn-primary" %>
                  <%= link_to "Do it later", root_path, class:"w-full md:w-auto btn btn-secondary" %>
                </div>

              </div>
          <% end %>
        </div>



    </div>

    <div class="order-first md:order-last p-4 md:p-12 flex flex-col">
      <%= image_tag "celebrate.png", class: "mx-auto my-auto h-24 md:h-auto", alt: "Person celebrating"  %>
    </div>

  </div>
</div>